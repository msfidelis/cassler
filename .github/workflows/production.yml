name: 'cassler ci'
on:
  push:
  pull_request:
    types: [ opened, reopened ]
jobs:
  unit-test:
    strategy:
      matrix:
        go-version: [1.15.x]
        platform: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.platform }}
    steps:

      - uses: actions/setup-go@v1
        with:
          go-version: ${{ matrix.go-version }}

      - uses: actions/checkout@v1

      - name: Install dependencies
        run: go get -u 

      - name: Test
        run: go test -v

  build-docker-artifacts:
    needs: [ unit-test ]
    runs-on: ubuntu-latest
    if: contains(github.ref, 'main')
    steps:
      - uses: actions/setup-go@v1
        with:
          go-version: '1.15.x'

      - uses: actions/checkout@v1

      - name: Dump latest git tag
        run: TAG=$(git describe --tags --abbrev=0)

      - name: Docker Build
        run: docker build -t cassler:latest .

      - name: Docker Tag Latest
        run: docker tag cassler:latest fidelissauro/cassler:latest

      - name: Docker Tag Latest Release
        run: docker tag cassler:latest fidelissauro/cassler:$TAG

      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD}}

      - name: Docker Push Latest
        run: docker push fidelissauro/cassler:latest

      - name: Docker Push Release Tag
        run: docker push fidelissauro/cassler:$TAG